[pytest]
# Preserve the archived-Halo convention that test.py encodes via its commented
# "# DISCONTINUED" imports: Halo suites stay in-tree but are never collected.
addopts = --ignore=GBotDiscord/test/halo --ignore=GBotDiscord/test/quart_api/halo_resource_test.py
