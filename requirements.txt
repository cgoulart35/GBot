# audioop was removed from the Python 3.13 stdlib (PEP 594); nextcord 2.4.2 imports it
# at module load, so the maintained backport is required on the python:3.13 image
audioop-lts==0.2.2; python_version >= "3.13"

debugpy==1.8.21
df2img==0.2.10
emoji==2.2.0
# firebase-admin hard-pins httpx[http2]==0.28.1, so the two pins below move together
firebase-admin==7.5.0
httpx==0.28.1
# nextcord >= 3 is required for pip-audit 0: 2.x imports pkg_resources at module load,
# which only setuptools <= 81 still ships, and all of those carry PYSEC-2026-3447
nextcord==3.2.0
nextcord-ext-menus==1.5.6
numpy==2.5.1
pandas==2.3.3
quart==0.21.0
quart-cors==0.7.0
yt-dlp==2026.7.4

Werkzeug==3.1.8
